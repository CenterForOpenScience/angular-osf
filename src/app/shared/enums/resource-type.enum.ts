export enum ResourceType {
  Null,
  File,
  Project,
  Registration,
  Preprint,
  ProjectComponent,
  Agent,
  DraftRegistration,
  Collection,
}

export enum CurrentResourceType {
  User = 'users',
  File = 'files',
  Project = 'nodes',
  Registration = 'registrations',
  Preprint = 'preprints',
}
